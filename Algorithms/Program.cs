using System;

namespace Algorithms
{
  class Program
  {
    static void Main(string[] args)
    {
      //int[] num = { 9, 8, 7, 6, 5, 4, 3, 2, 1, 0 };
      //num.PrintArray();
      //Sorter sorter = new Sorter();
      //sorter.InsertionSort(num).PrintArray();
      //sorter.MergeSort(num).PrintArray();
      //FizzBuzz();

      //Fibonacci f = new Fibonacci(15, 1000);
      //f.GenerateTillValue();
      //Console.WriteLine("\n");
      //f.GenerateNTerms();

      // Console.Write("test".ToCharArray().removeDuplicates());

      //StringOp.AreAnagrams("","");

      // "akhilbhardawaj".ToCharArray().removeDuplicates();

      Matrix.TestZeros();
      Console.Read();
    }

    
  }

 
}
